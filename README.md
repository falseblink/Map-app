# GPS Map Application

Это приложение React Native, созданное с помощью [Expo](https://expo.dev), которое включает в себя карту с возможностью добавления маркеров и современные функции GPS трекинга с локальными уведомлениями.

## 🚀 Основные функции

- **Интерактивная карта** с маркерами
- **GPS трекинг** - отображение текущего местоположения пользователя
- **Локальные уведомления** при приближении к сохраненным маркерам
- **Сохранение маркеров** с названием, описанием и изображениями
- **Автоматическое определение близости** к маркерам (в пределах 100 метров)
- **Реальное время обновления** местоположения
- **Система разрешений** для доступа к геолокации


Expo Go имеет ограничения по функциональности `expo-notifications`. Для тестирования уведомлений необходимо создать development build.

### GPS трекинг работает в Expo Go ✅
### Уведомления требуют Development Build 🚧

## 🛠 Установка и запуск

### Системные требования

1. **Node.js** (LTS версия)
2. **Expo Go** приложение на мобильном устройстве (только для базового тестирования)
3. **Development Build** для полного тестирования уведомлений

### Шаги установки

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd map-app
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте Development Build для полного тестирования:**
   ```bash
   # Создайте development build
   eas build --profile development --platform android
   # или для iOS
   eas build --profile development --platform ios
   ```

4. **Запустите приложение:**
   ```bash
   npx expo start
   ```

5. **Откройте приложение:**
   - **Для базового тестирования GPS**: Отсканируйте QR-код через **Expo Go**
   - **Для полного тестирования уведомлений**: Установите Development Build

## 🧪 Тестирование GPS и уведомлений

### Тестирование GPS в Expo Go ✅

GPS трекинг полностью работает в Expo Go:

1. **Запустите приложение** через Expo Go
2. **Разрешите доступ** к геолокации
3. **Проверьте отображение** вашего местоположения на карте
4. **Протестируйте обновления** в реальном времени

### Тестирование уведомлений - Development Build Required 🚧

Для тестирования уведомлений необходимо создать development build:

#### Создание Development Build:

```bash
# Настройте EAS CLI (если еще не установлен)
npm install -g @expo/eas-cli
eas login

# Создайте development build для Android
eas build --profile development --platform android

# Создайте development build для iOS
eas build --profile development --platform ios
```

#### Настройка проекта для Development Build:

1. **Обновите `app.json`:**
```json
{
  "expo": {
    "extra": {
      "eas": {
        "projectId": "your-project-id"
      }
    }
  }
}
```

2. **Создайте `eas.json`:**
```json
{
  "cli": {
    "version": ">= 2.0.0"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    }
  },
  "submit": {
    "production": {}
  }
}
```

#### Тестирование в Development Build:

1. **Установите build** на устройство
2. **Запустите приложение** из build
3. **Протестируйте уведомления** согласно сценариям ниже

### Подробное тестирование

#### Тестирование геолокации (работает в Expo Go):

**Через Expo Dev Tools:**
1. Включите **режим разработчика** на устройстве
2. Откройте **Expo Dev Tools** (http://localhost:19002)
3. Перейдите в раздел **"Location"**
4. Установите **тестовые координаты** для имитации движения

**Тестовые сценарии:**
- ✅ Разрешения на геолокацию - проверьте поведение при отказе
- ✅ Точность GPS - протестируйте разные уровни точности
- ✅ Обновления в фоне - проверьте работу при свернутом приложении
- ✅ Ошибки геолокации - симулируйте проблемы с GPS

#### Тестирование уведомлений (Development Build only):

**Основные сценарии:**
1. **Приближение к одному маркеру:**
   - Создайте маркер на карте
   - Подойдите к нему на расстояние менее 100 метров
   - Проверьте появление уведомления

2. **Приближение к нескольким маркерам:**
   - Создайте несколько маркеров рядом друг с другом
   - Подойдите к группе маркеров
   - Проверьте получение всех уведомлений

3. **Выход из зоны близости:**
   - После получения уведомления отойдите от маркера
   - Проверьте отмену уведомления

4. **Поведение при перезапуске:**
   - Перезапустите приложение
   - Проверьте сохранение состояния уведомлений

**Тестирование в различных состояниях:**
- **Активное приложение** - уведомления в foreground
- **Свернутое приложение** - уведомления в background  
- **Заблокированный экран** - уведомления при выключенном экране

## 📋 Архитектура проекта

```
map-app/
├── app/                      # Файловая навигация Expo Router
│   ├── _layout.tsx          # Главный layout с настройкой уведомлений
│   ├── index.tsx            # Главный экран с картой
│   └── marker/
│       └── [id].tsx         # Экран деталей маркера
├── components/              # React компоненты
│   ├── Map.tsx             # Компонент карты с GPS трекингом
│   ├── ImageList.tsx       # Список изображений маркера
│   └── MarkerContext.tsx   # Контекст управления маркерами
├── contexts/               # React Context провайдеры
│   └── DatabaseContext.tsx # Контекст базы данных
├── services/              # Бизнес-логика и API
│   ├── location.ts        # Сервис работы с геолокацией
│   └── notifications.ts   # Сервис управления уведомлениями
├── database/              # Слой доступа к данным
│   ├── schema.ts          # Схема базы данных SQLite
│   └── operations.ts      # Операции с базой данных
└── types.ts               # TypeScript типы и интерфейсы
```

## 🎯 Стратегия управления уведомлениями

### Принцип работы:
1. **Отслеживание в реальном времени** - проверка расстояния до всех маркеров при каждом обновлении GPS
2. **Предотвращение дубликатов** - использование Map для отслеживания активных уведомлений
3. **Автоматическая отмена** - удаление уведомлений при выходе из зоны близости
4. **Очистка старых уведомлений** - автоматическая очистка уведомлений старше 24 часов

### Технические особенности:
- **Threshold расстояния:** 100 метров (настраивается в `services/location.ts`)
- **Частота обновления GPS:** каждые 5 секунд или при перемещении на 5+ метров
- **Хранение состояния:** в памяти приложения через Map объект
- **Управление разрешениями:** автоматический запрос и проверка разрешений

## 🔧 Настройки и конфигурация

### Основные константы:
- `PROXIMITY_THRESHOLD` - радиус уведомления (100м по умолчанию)
- `timeInterval` - частота обновления GPS (5000ms)
- `distanceInterval` - минимальное расстояние для обновления (5м)

### Изменение параметров:
В файле `services/location.ts` можно настроить:
- Точность определения местоположения
- Частоту обновления
- Расстояние для триггера уведомлений

## 📱 Платформенные особенности

### iOS:
- Требуются разрешения на геолокацию в настройках
- Уведомления работают в background при включенном разрешении
- Поддержка точного и приблизительного определения местоположения
- **Development Build рекомендуется** для полной функциональности

### Android:
- Запрос разрешений runtime
- Background location требует дополнительных разрешений
- Оптимизация батареи может влиять на частоту обновлений
- **Development Build обязателен** для уведомлений

## ⚠️ Известные ограничения

### Expo Go ограничения:
1. **Уведомления не работают** в Expo Go (SDK 53+)
2. **GPS трекинг работает полностью** в Expo Go
3. **Development Build требуется** для тестирования уведомлений

### Общие ограничения:
1. **Хранение данных** - маркеры и изображения не сохраняются при перезапуске (требует интеграции AsyncStorage/API)
2. **Background трекинг** - ограничения платформ могут влиять на частоту обновлений
3. **Эмулятор** - GPS может работать неточно в эмуляторах
4. **Разрешения** - пользователь может отозвать разрешения в любой момент
5. **Сетевые требования** - для карт требуется интернет-соединение

## 🔮 Дополнительные функции (возможные улучшения)

- **Синхронизация с облаком** - сохранение данных в облаке
- **Экспорт/импорт маркеров** - обмен маркерами между пользователями
- **Треки маршрутов** - запись и отображение пройденных путей
- **Группировка маркеров** - организация маркеров по категориям
- **Push-уведомления** - уведомления от сервера
- **Аналитика** - статистика посещений и активности

## 🐛 Отладка

### Логирование:
- Проверьте консоль разработчика для сообщений о местоположении
- Логи уведомлений помогают отследить проблемы с разрешениями
- Expo Dev Tools показывает статус GPS подключения

### Частые проблемы:
- **"expo-notifications functionality is not fully supported in Expo Go"** - используйте Development Build
- **"Location access not granted"** - проверьте разрешения в настройках
- **Уведомления не приходят** - проверьте разрешения на уведомления
- **GPS не обновляется** - проверьте настройки геолокации устройства
- **Маркеры не сохраняются** - проблема с базой данных SQLite

### Решение проблем с Development Build:
```bash
# Очистите кеш
npx expo install --fix

# Пересоберите проект
eas build --clear-cache --profile development --platform android

# Или для iOS
eas build --clear-cache --profile development --platform ios
```

## 📞 Поддержка

Для получения помощи или сообщения об ошибках:
1. Проверьте этот README на наличие решения
2. Убедитесь, что используете Development Build для уведомлений
3. Изучите логи в консоли разработчика
4. Убедитесь в актуальности зависимостей: `npm install`

---

## Лицензия

MIT License - см. файл LICENSE для подробностей.